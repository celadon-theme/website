{
  "name": "celadon-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Celadon — theme showcase and submission site",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync-palette": "node scripts/sync-palette.mjs",
    "sync-wallpapers": "node scripts/sync-wallpapers.mjs",
    "test": "node --test scripts/*.test.mjs",
    "optimize-wallpapers": "node scripts/optimize-wallpapers.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
